[plugin]

name="AI Pipeline"
description="Exposes an in-editor HTTP tool server so an external AI agent (e.g. a local Qwen model) can inspect and author scenes, nodes, scripts, and project settings."
author="Periphery"
version="1.0"
script="plugin.gd"
